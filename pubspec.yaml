name: sams  # Tukar dari my_first_app ke sams
description: "Student Academic Management System"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.9.0   # TUKAR KE VERSI INI
  firebase_auth: ^6.4.0
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2
  change_app_package_name: ^1.5.0
  cloud_firestore: ^6.4.1
  file_picker: ^9.0.0     # <-- ADD THIS LINE
  pdf: ^3.11.1              # <-- ADD THIS LINE
  printing: ^5.13.2         # <-- ADD THIS LINE
  firebase_storage: ^13.4.1 # <-- UPDATE THIS TO 13.4.1
  moon_design: ^1.1.0
  intl: ^0.20.2
  flutter_stripe: ^11.5.0   # Card payments (FPX via Billplz pending)
  http: ^1.2.0              # Calls our backend to create Stripe PaymentIntents

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/LarianAmal.jpg
    - assets/Mobility.jpg
    - assets/Programming.jpg